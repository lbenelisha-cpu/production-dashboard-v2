# V4.8.20 – תיקון סנכרון תפוקה בין מחשבים

- תוקן מטמון IndexedDB של נתוני התפוקה: שינוי שורות בתוך אותו חודש מבטל כעת את המטמון הישן.
- חתימת המטמון כוללת את `dashboard_sync_version_v1`, ולא רק את רשימת החודשים.
- קריאות Netlify Blobs של `get` ו-`getMany` משתמשות ב-`consistency: strong`.
- כתיבה לענן מאומתת מול התוכן המדויק שנכתב.
- אין שינוי בחישובי התפוקה, במיפויים, באיכות, ביעדים או בדוחות.
